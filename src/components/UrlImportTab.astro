---
/**
 * URL import tab panel component
 *
 * Provides a URL input for importing SVG files from the web.
 *
 * @component
 */
---

<div class="mb-6">
    <label for="urlInput" class="block text-sm font-medium text-gray-700 mb-2">
        Import from URL
    </label>
    <input
        type="url"
        id="urlInput"
        placeholder="https://example.com/image.svg"
        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all"
        aria-label="Enter SVG file URL"
    />
    <p class="mt-2 text-xs text-gray-500">
        ⚠️ The URL must allow cross-origin requests (CORS)
    </p>
</div>
